echo "DS GEOLOCATION SERVERLESS SETUP --> now go home... so we can download all this stuff and make a mess there"
cd ~

echo "DS GEOLOCATION SERVERLESS SETUP --> setting up"
sudo yum update -y

sudo yum install gcc curl-devel -y
